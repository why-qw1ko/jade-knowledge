<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jade.mapper.ArticleMapper">

    <select id="selectArticleVOPage" resultType="com.jade.model.vo.ArticleVO">
        SELECT a.id, a.title, a.summary, a.cover_image, a.status,
               a.view_count, a.like_count, a.favorite_count, a.comment_count,
               a.is_top, a.create_time,
               c.name AS category_name,
               u.nickname AS author_name
        FROM article a
        LEFT JOIN category c ON a.category_id = c.id
        LEFT JOIN user u ON a.author_id = u.id
        WHERE a.deleted = 0
        <if test="categoryId != null">
            AND a.category_id = #{categoryId}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (a.title LIKE CONCAT('%', #{keyword}, '%') OR a.summary LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null">
            AND a.status = #{status}
        </if>
        ORDER BY a.is_top DESC, a.create_time DESC
    </select>

</mapper>
